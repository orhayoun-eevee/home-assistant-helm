skip-check:
  - CKV_K8S_40 # Home Assistant runs with explicit UID/GID to keep /config volume ownership predictable.
  - CKV_K8S_19 # Home Assistant defaults to hostNetwork for discovery compatibility (mDNS/SSDP); revisit after shared pod-network discovery approach is defined.
